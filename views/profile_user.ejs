<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile - Shopping App</title>
    <style>
        :root {
            --primary: #6C63FF;
            --secondary: #FF6584;
            --accent: #42C2FF;
            --light: #F8F9FA;
            --dark: #212529;
            --success: #28A745;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background-color: #f5f5f5;
            color: var(--dark);
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            border-bottom: 2px solid var(--primary);
            margin-bottom: 30px;
        }
        
        .logo {
            font-size: 28px;
            font-weight: bold;
            color: var(--primary);
        }
        
        .nav-links {
            display: flex;
            gap: 20px;
        }
        
        .nav-links a {
            text-decoration: none;
            color: var(--dark);
            font-weight: 500;
            padding: 8px 15px;
            border-radius: 20px;
            transition: all 0.3s ease;
        }
        
        .nav-links a:hover {
            background-color: var(--primary);
            color: white;
        }
        
        .nav-links .cart {
            background-color: var(--secondary);
            color: white;
        }
        
        .nav-links .cart:hover {
            background-color: #e04f6d;
        }
        
        .nav-links .orders {
            background-color: var(--accent);
            color: white;
        }
        
        .nav-links .orders:hover {
            background-color: #2fa8e6;
        }
        
        .nav-links .logout {
            background-color: var(--dark);
            color: white;
        }
        
        .nav-links .logout:hover {
            background-color: #343a40;
        }
        
        .order-item {
    display: flex;
    align-items: center;
    gap: 20px;
}

.product-image img {
    width: 80px;
    height: 80px;
    border-radius: 10px;
    object-fit: cover;
    border: 2px solid var(--primary);
}

        .profile-container {
            display: flex;
            gap: 30px;
            background-color: white;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin-bottom: 30px;
        }
        
        .profile-pic {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            border: 5px solid var(--primary);
        }
        
        .profile-info {
            flex: 1;
        }
        
        .profile-info h1 {
            color: var(--primary);
            margin-bottom: 10px;
        }
        
        .profile-info p {
            margin-bottom: 8px;
            color: #555;
        }
        
        .badge {
            display: inline-block;
            background-color: var(--accent);
            color: white;
            padding: 3px 10px;
            border-radius: 15px;
            font-size: 12px;
            margin-right: 5px;
            margin-top: 5px;
        }
        
        .stats {
            display: flex;
            gap: 20px;
            margin-top: 20px;
        }
        
        .stat-item {
            text-align: center;
            padding: 15px;
            border-radius: 10px;
            background-color: var(--light);
            flex: 1;
        }
        
        .stat-item h3 {
            color: var(--primary);
            margin-bottom: 5px;
        }
        
        .recent-orders {
            background-color: white;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            padding: 30px;
        }
        
        .recent-orders h2 {
            color: var(--primary);
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #eee;
        }
        
        .order-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            border-bottom: 1px solid #eee;
        }
        
        .order-item:last-child {
            border-bottom: none;
        }
        
        .order-info h4 {
            color: var(--dark);
            margin-bottom: 5px;
        }
        
        .order-info p {
            color: #777;
            font-size: 14px;
        }
        
        .order-status {
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 500;
        }
        
        .status-delivered {
            background-color: #d4edda;
            color: var(--success);
        }
        
        .status-pending {
            background-color: #fff3cd;
            color: #ffc107;
        }
        
        footer {
            text-align: center;
            padding: 20px;
            margin-top: 50px;
            color: #777;
            border-top: 1px solid #eee;
        }
        
        @media (max-width: 768px) {
            .profile-container {
                flex-direction: column;
                align-items: center;
                text-align: center;
            }
            
            .stats {
                flex-direction: column;
            }
            
            .nav-links {
                flex-wrap: wrap;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="logo">Profile</div>
            <div class="nav-links">
                  <a href="/" class="orders">📦 Home</a>
                <a href="/cart" class="cart">🛒 Cart</a>
                <a href="/order/<%=user._id%>" class="orders">📦 My Orders</a>
                <a href="/users/logout" class="logout">🚪 Logout</a>
            </div>
        </header>
        
        <div class="profile-container">
            <%if(user.Image){%>
                <a href="/users/profile/update"><img src="data:image/png;base64,<%= user.Image.toString('base64') %>"
                    alt="Profile Picture" class="profile-pic"></a>
            <%}else{%>
                <a href="/users/profile/update"><img src="https://randomuser.me/api/portraits/women/65.jpg" alt="Profile Picture" class="profile-pic"></a>
            <%}%>
           
            <div class="profile-info">
                <h1><%=user.full_name%></h1>
                <p>📧 <%=user.email%></p>
                <p><%=user.username%></p>
                <% if (addresses) { %>
                    <p>🏠 123, <%= addresses.street %>, <%= addresses.country %>, Karnataka - 560001</p>
                <% } else { %>
                    <p>🏠 No address found.</p>
                <% } %>
                
                
                <span class="badge">Premium Member</span>
                <span class="badge">Verified</span>
                
                
            </div>
        </div>
        
        <div class="recent-orders">
            <h2>Recent Orders</h2>
           
             <% if(product.length>0){%>
               <%product.forEach(function(val){%>
                <div class="order-item">
                    <div class="product-image">
                        <img src="data:image/png;base64,<%=val.image.toString('base64')%>" alt="Product Image">
                    </div>
                    <div class="order-info">
                        <h4><%=val.name%></h4>
                        <p>Order #12345 • <%=addresses.date%></p>
                    </div>
                    <div class="order-status status-delivered">Delivered</div>
                </div>
                
            <%})%>
            <%}else{%>

           <% }%>
          
        </div>
        
        <footer>
            <p>© 2023 ShopEasy. All rights reserved.</p>
        </footer>
    </div>
</body>
</html>
